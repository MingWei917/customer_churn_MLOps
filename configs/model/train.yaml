model:
  name: random_forest
  n_estimators: 300
  max_depth: 12
  min_samples_split: 5
  min_samples_leaf: 2
  random_state: 42
  n_jobs: -1

training:
  test_size: 0.2
  stratify: true
  debug_param: 1

output:
  model_path: models/model.pkl
  metrics_path: metrics/metrics.json
