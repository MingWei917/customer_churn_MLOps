model:
  name: logistic_regression
  params:
    random_state: 42
    max_iter: 5000
    C: 0.7
    solver: lbfgs

training:
  test_size: 0.2
  stratify: true

output:
  model_path: models/model.pkl
  metrics_path: metrics/metrics.json
